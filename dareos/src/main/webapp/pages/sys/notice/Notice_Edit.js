$(function(){	//初始化校验	InitFormValidator(0);	//添加浮动按钮层的监听	addFloatDivListener();	//保存监听。参数1：请求Url，参数2：主键Id，参数3：上帧url.保存成功后需要刷新上帧，	mtbSaveListener("edit","NOTICE_ID","toList","mainForm");	//如果需要额外的业务校验 只需在该页面增加方法 formCheckedEx();	var ue = UE.getEditor('editor');		var selId = parent.document.getElementById("selRowId").value;	if(null == selId || "" == selId){		var treeNodes = parent.leftcontent.getSelNodes();		if("" != treeNodes){		    var flag = treeNodes.def1;//判断是机构还是部门			if(1==flag){//部门			   var jgbhname = treeNodes.name;			   var index = jgbhname.indexOf("(");			   $("#BMMC").val(jgbhname.substring(index+1,jgbhname.length-1));			   $.ajax({		             url: "selectZTbyId?BMXXID="+treeNodes.id,		             type:"POST",		             dataType:"text",		             complete: function(xhr){		                eval("jsonResult = "+xhr.responseText);		                   if(jsonResult.success===true){		                       var arr = jsonResult.messages.split(',');		                       $('#JGMC').val(arr[0]);		                       $('#JGXXID').val(arr[1]);		                   }		             }	            });			}		}	}	var NOTICE_OBJ=$('#NOTICE_OBJ').val();	if(NOTICE_OBJ==1){		$('#NOTICE_OBJ_CHCECK').attr("checked", 'checked');	}     var NOTICE_DEPTStr=$('#NOTICE_DEPTS').val();     var NOTICE_DEPTS = NOTICE_DEPTStr.split(',');     for(var i=0;i<NOTICE_DEPTS.length;i++){         $("input[name='NOTICE_DEPT']").each(function (index, item) {                if(NOTICE_DEPTS[i]==$(this).val()){                    $(this).attr("checked", 'checked');                }         });     }});//如果需要额外的业务校验 只需在该页面增加方法 formCheckedEx();function formCheckedEx(){	var unitType = $("#NOTICE_TYPE").val();    var startTime = $("#STATIME").val();	var endTime = $("#ENDTIME").val();		if (!InputCheck($("#NOTICE_TITLE")[0])) {			return false;	}		if (unitType == "") {		chkCanErrMsg("", "请选择'公告类型'");		return false;	}			/*if (!InputCheck($("#ISSUER_NAME")[0])) {			return false;	}*/    if (!InputCheck($("#STATIME")[0])) {			return false;	}        if (!InputCheck($("#ENDTIME")[0])) {			return false;	}/*    if (!InputCheck($("#ATT_PATH")[0])) {			return false;	}*/     	if(endTime<startTime){		parent.showErrorMsg("'结束时间'不得小于'开始时间'");		return false;	}	// var NOTICE = $("#NOTICE").val();// 	var NOTICE = eWebEditor.getText();// 	NOTICE = $.trim(NOTICE);// 	if(NOTICE==null||NOTICE==''){// 	  parent.showErrorMsg("请输入'公告内容'");// 	  return false;// 	}// 	$("#mainForm")	//alert(UE.getEditor('editor').getAllHtml());    $("#NOTICE").val(UE.getEditor('editor').getContent());	//判断是否勾选了经销商可见	if($("#NOTICE_OBJ_CHCECK:checked").val()==undefined){		$("#NOTICE_OBJ").val("2");	}else{		$("#NOTICE_OBJ").val("1");	}	//获取选择的账套信息	var vals = [];	$("input[name='NOTICE_DEPT']:checked").each(function (index, item) {		        vals.push($(this).val());	 });	if(vals.length==0){		parent.showErrorMsg("请至少选择一个组织！");		return false;	}	$("#NOTICE_DEPTS").val(vals);	return true;} 